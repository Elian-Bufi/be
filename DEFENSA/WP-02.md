# DEFENSA — WP-02 · Identidad y sesiones

> Paquete ejecutado el 2026-09-18 y 19, bajo **ACTA-DIR-034 v1.0**. Definición por IDs registrada **antes del primer commit de código**: `docs/paquetes/WP-02.md` (commit `b9242a3`, PR #6).
> Especificación consolidada con citas `archivo:línea`, producida por lectores independientes del legajo y un verificador adversarial. Tensiones en `docs/DEUDA_LEGAJO.md` (DL-009 a DL-028).

## 1. Qué hace el paquete

Una persona sintética crea su cuenta aceptando **términos (A1)** y recibiendo la **información de privacidad (A2)** como dos actos separados. Después inicia sesión, ve sus datos no sensibles (incluido que el tratamiento de datos de salud, **A3, no está otorgado**), cierra sesión y puede cerrar su cuenta. El recorrido funciona en el website y en el APK.

| Superficie | Dónde | Qué hay |
|---|---|---|
| API | `https://be-api-hndp.onrender.com/api/v1` | ACC-01 `POST /registrations` · ACC-02 `POST /auth/sessions` · ACC-03 `DELETE /auth/sessions/current` · ACC-04 `DELETE /auth/sessions` · ACC-05 `GET /me` · ACC-P1-03 `POST /me/account-closure-requests` · CON-05 `GET /me/health-data-consent-requirement` (solo lectura). Contrato: `docs/api/openapi.json`, generado desde `@be/domain` |
| Website | `https://be-web-1ngj.onrender.com` | `/register` · `/login` · `/account` · `/legal/terminos` · `/legal/privacidad` |
| APK | release `be-apk-0.2.0` (§6) | Bienvenida · Crear cuenta · Iniciar sesión · Cuenta (Estado · Privacidad · Seguridad · Cerrar mi cuenta) |

## 2. Dónde vive cada garantía

Cada garantía tiene al menos dos capas: el código de aplicación la decide y la base la sostiene aunque el código se equivoque.

### 2.1 Anti-enumeración (D2 · TEST-AUTH-001)

| Capa | Archivo · función | Qué hace |
|---|---|---|
| Decisión | `apps/api/src/sesion/sesion.service.ts` · `SesionService.iniciar` | Identificador inexistente, contraseña incorrecta, credencial suprimida (cuenta cerrada) y cuenta no operativa ejecutan el **mismo trabajo** (una búsqueda, una verificación bcrypt y una escritura de auditoría) y responden el **mismo** `401 INVALID_CREDENTIALS`. La causa real queda solo en la auditoría interna. |
| Tiempo | `apps/api/src/plataforma/credenciales.service.ts` · `CredencialesService.verificar` | Siempre hace exactamente una verificación bcrypt; sin credencial, compara contra un **hash señuelo** generado al arrancar. |
| Registro | `apps/api/src/identidad/registro.service.ts` · `RegistroService.registrar` | El hash se calcula antes de saber si el correo existe. `409 REGISTRATION_NOT_AVAILABLE` es neutral: no dice «ya existe una cuenta» (el 409 en sí es un oráculo que el 09 impone; DL-010). |
| Límite | `apps/api/src/plataforma/limitador.service.ts` · `LimitadorService.consumir` | `429 RATE_LIMITED` idéntico exista o no la cuenta (login por IP + identificador; registro por IP). |
| Prueba | `test/integration/sesiones.int-spec.ts` | Cuatro casos: inexistente, contraseña incorrecta, SUSPENDIDA y CERRADA con la contraseña correcta. Mismo código, cuerpo y headers, y ninguna sesión creada. Las medianas de tiempo quedan dentro de max(50 ms, 35 %) (DL-014). En local, 75,6 / 78,8 / 78,8 / 80,7 ms. |

### 2.2 Autorización server-side por operación

| Capa | Archivo · función | Qué hace |
|---|---|---|
| Sesión | `apps/api/src/sesion/sesion.guard.ts` · `SesionGuard.canActivate` | En **cada** request verifica la firma HS256 con algoritmo fijo, que la fila `Sesion` exista, sea del mismo titular y esté ACTIVA, la expiración, que la `versionDeControl` coincida con `ControlDeSesion.version` y que la cuenta esté OPERATIVA. El actor sale del token verificado y de la base, nunca del cliente. |
| Step-up | `apps/api/src/identidad/cierre.service.ts` · `CierreService.solicitar` | El cierre exige una sesión autenticada hace 10 minutos o menos (SESSION_STEP_UP, DL-017); si no, `403 STEP_UP_REQUIRED`. |
| Entrada | `apps/api/src/http/validacion.ts` · `validarCuerpo` | Schemas estrictos: cualquier campo del cliente que no esté en el contrato (rol, estado, capacidades, A3) da `400 UNKNOWN_FIELD` (TEST-AUTH-009). |
| Superficie | `packages/domain/src/procedencia.ts` · `superficieDeclarada` | `X-BE-Surface` solo se registra como procedencia; **nunca autoriza** (DL-022). |

### 2.3 Corte prospectivo (en WP-02: el cierre corta hacia adelante)

A3 no se otorga en WP-02, así que su revocación no existe todavía (API-CON-06/07/08 quedan fuera). El corte prospectivo que sí se implementa es el del cierre de cuenta (D4):

| Capa | Archivo · función | Qué hace |
|---|---|---|
| Máquina de estado | `packages/domain/src/estado-operativo-de-cuenta.ts` · `evaluarTransicionDeCuenta` | Lista blanca literal del 06 §5.7.4 con **guardas explícitas**. `CerrarCuenta` exige titular con sesión válida y reciente, la versión de consecuencias presentada y confirmación explícita. `SUSPENDIDA → CERRADA` no existe. |
| Efectos | `apps/api/src/identidad/estado-de-cuenta.service.ts` · `EstadoDeCuentaService.transicionar` | En una transacción: `SELECT … FOR UPDATE`, evaluación, estado nuevo, **revocación de todas las sesiones** y avance de la versión de control (`revocarSesiones`). Después suprime el hash de la credencial, con el registro de supresión antes. Revoca A1 (08 §12.4) y emite `CuentaCerrada` con estado anterior y resultante. |
| Base | `migration.sql` · `be_identidad_guardar` | Rechaza toda transición no declarada (`TRANSICION_NO_DECLARADA SUSPENDIDA -> CERRADA`), nacer en otro estado que no sea OPERATIVA y el DELETE. |
| Pruebas | `test/integration/cierre.int-spec.ts` · `maquina-de-estado.int-spec.ts` | **TEST-AUTH-011:** después del cierre, un login con la contraseña correcta da 401 neutral y no crea filas. **TEST-AUTH-012:** las sesiones previas de web y APK y la que ejecutó el cierre, con token vigente, dan 401 en la request siguiente. Además, una prueba por transición declarada y el rechazo de `SUSPENDIDA → CERRADA` en el servicio, en la API y en la base. |

### 2.4 Historia inmutable; la ausencia se declara

| Capa | Archivo · función | Qué hace |
|---|---|---|
| Base | `migration.sql` · `be_solo_agregar` | Eventos, auditoría, supresiones, solicitudes de cierre, versiones de texto, idempotencia y métodos de acceso son **append-only**. `TRUNCATE` también está bloqueado. |
| Base | `migration.sql` · `be_acto_registrable_guardar` | La evidencia de un acto es inmutable; la única transición es `VIGENTE → REVOCADO` para los tipos revocables. |
| Base | `migration.sql` · `be_identidad_con_perfil_propio` | INV-06-22: una Identidad sin Perfil propio no se puede confirmar (constraint trigger diferido). |
| Base | `migration.sql` · `be_credencial_local_guardar` | El hash solo se suprime si la cuenta está CERRADA y la supresión ya quedó asentada (08 R-02, §18). |
| Ausencia | `apps/api/src/consentimiento/consentimiento.service.ts` · `ConsentimientoService.requisito` | Sin acto A3, `currentConsent: null`: el concepto existe y no tiene valor (09v7 T11). No hay booleano «a3Otorgado» en ninguna tabla. |
| Pruebas | `cierre.int-spec.ts` (TEST-AUTH-013 b) · `schema.int-spec.ts` | Después del cierre siguen la identidad, el perfil, el método, A1 (revocado) y A2, y los eventos. Un DELETE directo sobre identidad, actos y eventos es rechazado por la base. |

### 2.5 Demostrables de la definición

| # | Demostrable | Dónde |
|---|---|---|
| D1 | Registrarse no concede A3; A1 y A2 son actos separados | `RegistroService.crear`: dos filas `ActoRegistrable` (`ACTOS_DEL_REGISTRO` = TERMINOS, PRIVACIDAD_INFO), cada una con versión, hash, finalidad, superficie, IP/UA, actor, autoría y procedencia. **TEST-AUTH-002:** cero actos DATOS_SALUD_BE y CON-05 con `currentConsent: null`. UI: dos casillas separadas, nunca premarcadas |
| D2 | Login indistinguible | §2.1 |
| D3 | Registro concurrente con el mismo correo crea una sola identidad, por **índice único** | `prisma/schema.prisma` `@@unique([tipo, referencia])` → `metodo_de_acceso_tipo_referencia_key` en la migración. `RegistroService`: `P2002` → 409 neutral. **TEST-RF-001:** 8 registros concurrentes → 1 identidad, 7 × 409; una inserción directa en la base es rechazada por el índice |
| D4 | El cierre impide sesiones nuevas e invalida las actuales, sin borrar historia | §2.3 y §2.4 |

## 3. Tres preguntas que haría un tribunal

### P1. «Si el login tarda distinto cuando la cuenta no existe, ¿no se puede enumerar por tiempo?»

No en la medida que se puede probar. El trabajo es el mismo en todas las ramas: `SesionService.iniciar` hace una búsqueda por el índice único, una verificación bcrypt y una escritura de auditoría, exista o no la cuenta. Cuando no hay hash, `CredencialesService.verificar` compara contra un hash señuelo del mismo costo, generado al arrancar el proceso. La prueba TEST-AUTH-001 mide 12 vueltas intercaladas de cuatro casos (inexistente, contraseña incorrecta, SUSPENDIDA y CERRADA) y exige que las medianas no se separen más de max(50 ms, 35 %). La tolerancia es una decisión propia porque el legajo no fija una: está en DL-014. Lo que **no** se afirma: que sea imposible distinguir con millones de muestras en la misma red. Para eso está el límite de 5 intentos cada 15 minutos por IP e identificador, con un 429 también neutral.

### P2. «¿Qué impide que un error de programación cierre una cuenta suspendida, o borre historia?»

Tres capas independientes. La primera es `@be/domain`: `evaluarTransicionDeCuenta` busca la transición en la lista blanca del 06 §5.7.4 por nombre y origen, y `CerrarCuenta` solo está declarada desde OPERATIVA. La segunda es la API: una cuenta suspendida no tiene sesión válida (el guard exige OPERATIVA), así que ni siquiera llega al servicio. La tercera es la base: el trigger `be_identidad_guardar` rechaza cualquier `UPDATE` de estado fuera de la lista blanca aunque venga de SQL directo, y los triggers de historia rechazan `UPDATE`, `DELETE` y `TRUNCATE`. La prueba `maquina-de-estado.int-spec.ts` ataca las tres capas por separado: servicio, endpoint y `UPDATE identidad SET estado_operativo_de_cuenta = 'CERRADA'` sobre una suspendida.

### P3. «Si la sesión es un JWT, ¿cómo se invalida antes de que expire?»

El JWT solo identifica una fila: lleva `sid` (la sesión), `sub` (el titular) y `tv` (la versión de control con la que se emitió). La verdad está en la base, y `SesionGuard` la consulta en **cada** request: la fila tiene que estar ACTIVA, la versión tiene que coincidir con la actual de la cuenta y la cuenta tiene que estar OPERATIVA. Cerrar sesión, cerrar todas, suspender o cerrar la cuenta cambian la fila y avanzan la versión. Así, la request siguiente falla con 401 aunque el token no haya vencido. Es lo que exige el 08 §26.1: «un token puramente stateless sin lista de revocación no satisface esta política». TEST-AUTH-012 lo prueba con tres sesiones vigentes (web, APK y la que ejecutó el cierre).

## 4. Qué quedó fuera

| Ítem | Referencia |
|---|---|
| **TEST-AUTH-013 (a)** «finaliza vínculos por eventos»: **BLOCKED**. No hay modelo de vínculos; con V01 el cierre no inventa eventos relacionales | DL-018 · 05:14520-14522 |
| Otorgar, historial y revocación de A3 | API-CON-06/07/08 · RF-020 |
| Edición del perfil propio (no hay campos aprobados) | API-ACC-06 · DL-009 · RF-017 |
| Estado de la solicitud de cierre (el cierre es síncrono) | API-ACC-P1-04 · DL-016 |
| Recuperación de acceso | RF-005 · UC-E09 · API-ACC-P1-01/02 |
| Identidad federada y métodos múltiples | RF-003 · RF-004 · UC-E05/E06 |
| MFA | 08 §25 |
| Perfil profesional, especialidades y verificación | RF-008…014 · UC-P01 |
| Vínculos y B2 | RF-018…025 · UC-P06/P07 |
| Alta por invitación; declaración de mayoría de edad | 08 §24.3, §24.7 · DL-023 |
| Operación administrativa de suspensión (solo existe como servicio interno, probado) | DL-020 |
| Refresh rotativo de sesión | 07 §43-bis · DL-012 |
| Retención y anonimización posteriores al cierre | 08 §16–§18 · DL-019 |
| Textos legales reales (los de WP-02 son sintéticos, versionados y con hash) | 08 R-08-05, §42-1 · DL-028 |

## 5. Línea de control

**¿Se agregó algo a `DEUDA_LEGAJO.md`?** Sí: **veinte entradas, DL-009 a DL-028**, registradas antes del primer commit de código, cada una con lo que dice el legajo, por qué no se puede aplicar tal cual y dos opciones. **DL-003 queda cerrada:** la estructura mínima de Identidad BE del 06 §5.4.2 existe completa, con Perfil propio 1:1 garantizado por la base. DL-005 suma un hallazgo: `500 INTERNAL_ERROR` sí aparece en el 09 (09v7:185) y se adopta.

## 6. Evidencia

*(se completa al cierre del paquete)*
